Copy and paste these prompts directly to your AI one by one:1. The Database Schema & Relational Integrity"You've built the frontend UI, but I need to see the exact database schema powering this. Please provide the SQL or ORM models that connect a student, a class session, and one of our 20 staff tutors. Specifically, how does the system prevent a double-booking if a tutor is scheduled for two different classes at the exact same time?"2. State Persistence & Cloudflare Workers"We are running the continuous-path whiteboard on a Cloudflare Worker via WebSockets. If the Worker restarts or the Durable Object hibernates during a live session, what is the exact programmatic mechanism that saves the current canvas state and restores it when the clients reconnect so they don't lose their work?"3. Multi-Tenancy & Data Security"Show me the exact backend middleware that enforces data isolation. If a student manipulates the API request payload and changes their classId to a class they are not enrolled in, which file and line of code catches that unauthorized access and blocks the database query?"4. Gemma 4 AI Derivation Gating"Regarding the AI derivation panel, show me the backend route that securely proxies the Gemma 4 API. How are you guaranteeing that this endpoint is strictly gated so only the educator role can trigger AI requests, preventing students from bypassing the UI and hitting the API directly to get the answers?"5. Global Error Handling & Rollbacks"Assume a tutor clicks 'End Session', which needs to save the whiteboard snapshot, log the attendance, and update the payroll ledger. If the payroll update fails midway through the transaction, show me the exact database rollback logic that prevents the attendance and whiteboard snapshot from being left in an orphaned or corrupted state."How to Evaluate the AI's ResponseWhen the AI replies, use this matrix to grade whether it actually built the system or is just bluffing:What the AI ProvidesYour EvaluationNext StepSQL Schemas, Middleware Code, Transaction BlocksPass. It has actually modeled the complex business logic and data relationships.Move on to the next phase of development.Generic Explanations ("The system will use...")Fail. It is describing what should happen but hasn't written the code for it.Reply: "Stop explaining the concept. Write the actual code for this mechanism right now."Frontend UI State (useState, React Context)Fail. It is trying to handle secure backend operations on the client side, which is a massive security flaw.Reply: "This must be handled on the backend. Move this logic to our Vercel API routes."