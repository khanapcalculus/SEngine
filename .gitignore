# dependencies
/node_modules

# next.js
/.next
/out
/build

# vercel
.vercel

# env / secrets — never commit
.env
.env*.local
.env.local
.env.production

# typescript / build info
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.log
.env*

# secrets / recovery codes — never commit
recovery-codes.txt
*recovery*.txt

# loose scratch / secret-bearing notes accidentally created in repo root — never commit
/fe_oa_*.txt
/AI_PROVIDER*.txt
/Untitled.txt
/Summary of *.txt
/httpsimage*.txt
