{
  "name": "sengine-whiteboard-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object for the SEngine RTC whiteboard",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^6.0.3",
    "wrangler": "^3.78.0"
  }
}
